#include <stdio.h>
#include<math.h>
int main (void)
{
    int maiorMultiplo, expoente=0, base, bit;
    double numero, resto;
    printf("Informe a base:\n");
    scanf("%d",&base);
    printf("informe o numero a converter:\n");
    scanf("%lf",&numero);
    for(;maiorMultiplo<numero;expoente++)
    {
        maiorMultiplo=pow(base, expoente);
        
    }
    int resposta[expoente];
    
}